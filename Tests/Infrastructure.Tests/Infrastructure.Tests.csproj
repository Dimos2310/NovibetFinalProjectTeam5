<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Sqlite (not the EF InMemory provider) on purpose: InMemory doesn't reliably
         enforce unique indexes/FKs, and the whole point of these tests is to prove
         the real constraints (unique Address, unique TwoLetterCode, the FK) and the
         race-condition handling in the repositories actually work. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.10" />
<<<<<<< HEAD
    <!-- Pin transitively-pulled SQLitePCLRaw above 2.1.11: that version bundles a SQLite
         build older than 3.50.2, which has a known high-severity memory corruption CVE
         (GHSA-2m69-gcr7-jv3q). This direct reference overrides the transitive one. -->
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="3.0.4" />
=======
    <PackageReference Include="Microsoft.Extensions.Caching.Memory" Version="10.0.10" />
>>>>>>> origin/cache-tests
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Infrastructure\Infrastructure.csproj" />
    <ProjectReference Include="..\..\Application\Application.csproj" />
    <ProjectReference Include="..\..\Domain\Domain.csproj" />
  </ItemGroup>

</Project>
